version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: socialpulse
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  server:
    build: ./socialPulse-app/backend
    ports: []
    env_file:
      - path: ./socialPulse-app/backend/.env
        required: false
    environment:
      DATABASE_URL: postgresql://postgres:password@postgres:5432/socialpulse
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ""
      NODE_ENV: development
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes: []

  client:
    build: ./socialPulse-app/frontend
    ports: []
    env_file:
      - path: ./socialPulse-app/frontend/.env
        required: false
    environment:
      VITE_API_URL: http://localhost:5000/api
    depends_on:
      - server
    volumes: []

volumes:
  postgres_data:
  redis_data:
